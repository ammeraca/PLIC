import {StyleSheet} from "react-native";

export const container = StyleSheet.create({
    simple_centered: {
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
    },
});
